NEXT_PUBLIC_APP_NAME=Vectra
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Supabase Auth (free tier)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Server only — cloud plant data + telemetry ingest (see docs/SUPABASE_SETUP.md)
SUPABASE_SERVICE_ROLE_KEY=

# AI alert briefs (server only — never expose to the browser)
OPENAI_API_KEY=

# Solana seal attestation (server only)
NEXT_PUBLIC_SOLANA_CLUSTER=devnet
SOLANA_SECRET_KEY=
# Optional custom RPC
# SOLANA_RPC_URL=

